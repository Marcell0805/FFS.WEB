# FFS Web — V0 Prototype

In-memory Blazor prototype for exploring FFS on the desktop/web.

## Run

```powershell
cd "D:\repos\FFS Web"
dotnet run --project src/FFS.Web
```

Open the URL shown in the console (default `http://localhost:5049`).

No database. No authentication. Demo data is seeded in memory on startup.
